# Модуль 5. Сервисы фильтрации пакетов и трансляции адресов

## Введение: Зачем нужна фильтрация и трансляция?

Представьте, что вы охраняете вход в здание:
- **Фильтрация** — вы проверяете, кто входит (разрешенные сотрудники) и что они несут (безопасность).
- **NAT** — вы меняете "пропуска" так, чтобы внешний мир видел только один вход, а внутри было много людей (хосты) с разными кабинетами.

**Межсетевой экран (Firewall)** — это система безопасности, контролирующая трафик между сетями.
**NAT (Network Address Translation)** — механизм преобразования IP-адресов, позволяющий экономить адресное пространство и скрывать внутреннюю структуру сети.

---

## Часть 1. Обзор типов межсетевых экранов

### 1.1 Классификация по архитектуре

```mermaid
graph TB
    subgraph Типы_межсетевых_экранов
        PACKET[Пакетные фильтры<br/>Packet Filters]
        STATE[С отслеживанием состояния<br/>Stateful Firewalls]
        PROXY[Прокси-серверы<br/>Application Gateways]
        NGFW[Межсетевые экраны<br/>следующего поколения<br/>NGFW]
    end
    
    subgraph Особенности
        P1[Анализирует заголовки<br/>Уровень 3-4]
        S1[Отслеживает соединения<br/>Уровень 3-5]
        PR1[Анализирует содержимое<br/>Уровень 7]
        N1[DPI + IPS + SSL<br/>Многофункциональный]
    end
    
    PACKET --- P1
    STATE --- S1
    PROXY --- PR1
    NGFW --- N1
    
    style PACKET fill:#ffcdd2,stroke:#c62828
    style STATE fill:#fff9c4,stroke:#fbc02d
    style PROXY fill:#e1f5fe,stroke:#01579b
    style NGFW fill:#c8e6c9,stroke:#2e7d32
```

### 1.2 Сравнение типов межсетевых экранов

| Тип | Уровень OSI | Принцип работы | Преимущества | Недостатки |
|-----|-------------|----------------|--------------|------------|
| **Пакетный фильтр** | 3-4 | Проверяет заголовки IP, TCP, UDP | Быстрый, простой | Не анализирует контекст |
| **Stateful** | 3-5 | Отслеживает состояние соединений | Безопаснее, понимает контекст | Ресурсоемкость |
| **Прокси** | 7 | Посредник для приложений | Глубокий анализ контента | Медленный, сложный |
| **NGFW** | 2-7 | DPI, IPS, приложения | Максимальная защита | Дорогой, сложный |

### 1.3 Топологии развертывания

```mermaid
graph TB
    subgraph Топология_с_двумя_интерфейсами
        INTERNET[Интернет] --> FW1[Межсетевой экран]
        FW1 --> LAN1[Локальная сеть]
    end
    
    subgraph Топология_с_DMZ
        INTERNET2[Интернет] --> FW2[Межсетевой экран]
        FW2 --> DMZ[DMZ<br/>Веб-серверы]
        FW2 --> LAN2[Локальная сеть]
    end
    
    subgraph Топология_с_несколькими_сегментами
        INTERNET3[Интернет] --> FW3[Межсетевой экран]
        FW3 --> VLAN10[VLAN 10<br/>Сотрудники]
        FW3 --> VLAN20[VLAN 20<br/>Гости]
        FW3 --> VLAN30[VLAN 30<br/>Серверы]
        FW3 --> DMZ2[DMZ]
    end
```

---

## Часть 2. Принципы работы пакетных фильтров

### 2.1 Базовый принцип

**Пакетный фильтр** анализирует каждый пакет изолированно, без учета контекста предыдущих пакетов.

```mermaid
graph LR
    subgraph Пакет
        SRC[Source IP<br/>192.168.1.10]
        DST[Dest IP<br/>8.8.8.8]
        SPORT[Source Port<br/>54321]
        DPORT[Dest Port<br/>53]
        PROTO[Protocol<br/>UDP]
        FLAGS[TCP Flags]
    end
    
    subgraph Правило_фильтрации
        COND{Соответствует<br/>правилам?}
    end
    
    subgraph Действие
        ACCEPT[ACCEPT<br/>пропустить]
        DROP[DROP<br/>отбросить]
        REJECT[REJECT<br/>отклонить с уведомлением]
    end
    
    SRC --> COND
    DST --> COND
    SPORT --> COND
    DPORT --> COND
    PROTO --> COND
    
    COND -->|Да| ACCEPT
    COND -->|Нет| DROP
```

### 2.2 Структура правил фильтрации

Типичное правило пакетного фильтра состоит из:

```
[Действие] [Протокол] [Source IP] [Source Port] [Destination IP] [Destination Port] [Flags]
```

**Примеры правил (iptables):**

```bash
# Разрешить SSH только с конкретного IP
iptables -A INPUT -p tcp --dport 22 -s 192.168.1.100 -j ACCEPT

# Разрешить HTTP всем
iptables -A INPUT -p tcp --dport 80 -j ACCEPT

# Запретить все остальное
iptables -A INPUT -j DROP
```
